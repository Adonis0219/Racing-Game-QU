using UnityEngine;

public class Tumbleweed : MonoBehaviour
{
   
    // Update is called once per frame
    void Update()
    {
        //transform.Rotate(Vector3.left,120f*Time.deltaTime);
        //transform.rotation *= Quaternion.Euler(-30f*Time.deltaTime, 0, 0);
    }
}
